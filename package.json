{
  "name": "nocturne-weave",
  "version": "1.0.0",
  "description": "A dark, atmospheric storytelling platform.",
  "scripts": {
    "build": "mkdir -p dist && cp index.html manifest.json icon.svg _redirects CNAME dist/ && touch dist/.nojekyll && cp index.html dist/404.html"
  },
  "dependencies": {}
}